// WRITE A FUNCTION TO GET THE COMPUTER'S CHOICE
function getComputerChoice() {
    const randomChoice = Math.floor(Math.random() * 3);
    if (randomChoice === 0) {
        return 'Rock';
    } else if (randomChoice === 1) {
        return 'Paper';
    } else {
        return 'Scissor';
    }
}

// WRITE A FUNCTION TO GET THE PLAYER'S CHOICE
function getPlayerChoice() {
    let playerChoice = prompt('Select your weapon', '');
    return playerChoice = playerChoice.charAt(0).toUpperCase() + playerChoice.slice(1).toLowerCase();
}

// WRITE A FUNCTION THAT PLAYS A SINGLE ROUND OF ROCK PAPER SCISSORS.
let playerScore = 0;
let computerScore = 0;
let tieScore = 0;
//variables above are declared to be used for counting score at the end of match and each round

function singleRound(playerSelection, computerSelection) {
    if (playerSelection === 'Rock'  && computerSelection === 'Scissor') {
        playerScore++;
        return 'You Won! Rock beats Scissor';        
    } else if (playerSelection === 'Paper' && computerSelection === 'Rock') {
        playerScore++;
        return 'You Won! Paper beats Rock';
    } else if (playerSelection === 'Scissor' && computerSelection === 'Paper') {
        playerScore++;
        return 'You Won! Scissor beats Paper';
    } else if (playerSelection === 'Rock' && computerSelection === 'Rock') {
        tieScore++;
        return 'Round tied!';
    } else if (playerSelection === 'Paper' && computerSelection === 'Paper') {
        tieScore++;
        return 'Round tied!';
    } else if (playerSelection === 'Scissor' && computerSelection === 'Scissor') {
        tieScore++;
        return 'Round tied!';
    } else if (playerSelection === 'Scissor'  && computerSelection === 'Rock') {
        computerScore++;
        return 'You Lost! Rock beats Scissor';        
    } else if (playerSelection === 'Rock' && computerSelection === 'Paper') {
        computerScore++;
        return 'You Lost! Paper beats Rock';
    } else if (playerSelection === 'Paper' && computerSelection === 'Scissor') {
        computerScore++;
        return 'You Lost! Scissor beats Paper';
    }
}

// WRITE A FUNCTION CALLED GAME() THAT KEEPS A SCORE AND REPORTS WINNER AT THE END
function game() {
    let playerSelection;
    let computerSelection;
    
    playerSelection = getPlayerChoice();
    computerSelection = getComputerChoice();
    console.log(singleRound(playerSelection, computerSelection));

    playerSelection = getPlayerChoice();
    computerSelection = getComputerChoice();
    console.log(singleRound(playerSelection, computerSelection));

    playerSelection = getPlayerChoice();
    computerSelection = getComputerChoice();
    console.log(singleRound(playerSelection, computerSelection));

    if (playerScore === 3) {
        return console.log('MEGA JOB! You have defeated the computer');
    } else if (computerScore === 3) {
        return console.log('CRY NOW! Computer has beaten you!');
    }

    playerSelection = getPlayerChoice();
    computerSelection = getComputerChoice();
    console.log(singleRound(playerSelection, computerSelection));

    if (playerScore === 2 && tieScore === 2) {
        return console.log('MEGA JOB! You have defeated the computer');
    } else if (computerScore === 2 && tieScore === 2) {
        return console.log('CRY NOW! Computer has beaten you!');
    } else if (playerScore === 3) {
        return console.log('MEGA JOB! You have defeated the computer');
    } else if (computerScore === 3) {
        return console.log('CRY NOW! Computer has beaten you!');
    }

    playerSelection = getPlayerChoice();
    computerSelection = getComputerChoice();
    console.log(singleRound(playerSelection, computerSelection));

    if (playerScore > computerScore) {
        return console.log('MEGA JOB! You have defeated the computer');
    } else if (computerScore > playerScore) {
        return console.log('CRY NOW! Computer has beaten you!');
    } else if (playerScore === computerScore) {
        return console.log('BOOORIN! Game tied');
    }
}
game();

// live commentory: you are ahaed; sum of all scores and then using comparison to report the game 
// invalid input
